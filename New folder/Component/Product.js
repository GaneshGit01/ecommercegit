import React from 'react'
import { useNavigate } from 'react-router-dom';

function Product() {
    // const Navigate = useNavigate();
  return (
    <div>
      <h2>Product</h2>
        {/* <button onClick={()=>{Navigate("/product")}}>About</button> */}
    </div>
  )
}

export default Product;